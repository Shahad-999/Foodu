import 'package:flutter/material.dart';

class TrackDriverBody extends StatelessWidget {
  const TrackDriverBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('tracking driver ......'));
  }
}
